services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: asadzedeh
      POSTGRES_PASSWORD: asadzedeh
      POSTGRES_DB: asadzedeh
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U asadzedeh"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
