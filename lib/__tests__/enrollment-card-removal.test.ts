import fs from "node:fs";import path from "node:path";import{describe,expect,it}from"vitest";
describe("enrollment card removal",()=>{it("has no live feature files or references",()=>{expect(fs.existsSync("app/api/enrollment-card/route.ts")).toBe(false);expect(fs.existsSync("lib/enrollment-card-pdf.tsx")).toBe(false);const roots=["app","components","lib"];const files:string[]=[];const walk=(dir:string)=>{for(const entry of fs.readdirSync(dir,{withFileTypes:true})){const full=path.join(dir,entry.name);if(entry.isDirectory()){if(entry.name!=="__tests__")walk(full)}else if(/\.(ts|tsx)$/.test(entry.name))files.push(full)}};roots.forEach(walk);const source=files.map(file=>fs.readFileSync(file,"utf8")).join("\n");expect(source).not.toMatch(/enrollment-card|generateEnrollmentCardPdf|دانلود کارت ورود|کارت ورود به کلاس/);expect(source).toContain("generateCertificatePdf")})});
