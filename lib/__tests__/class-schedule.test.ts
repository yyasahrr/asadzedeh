import fs from"node:fs";import{describe,expect,it}from"vitest";import{classCategory,currentJalaliMonth,deriveClassScheduleStatus,jalaliMonthGrid}from"../class-schedule";import type{InPersonClass}from"../types";
const base:InPersonClass={slug:"x",title:"گلیم‌بافی (حضوری)",instructor:"استاد",startDate:"2099-01-01",days:"شنبه",time:"10",sessions:2,capacity:10,remaining:8,location:"کارگاه",price:1,image:"/x",excerpt:"",includes:[]};
describe("class schedule",()=>{it("derives open, limited and full",()=>{expect(deriveClassScheduleStatus(base,new Date("2026-01-01"))).toBe("open");expect(deriveClassScheduleStatus({...base,remaining:2},new Date("2026-01-01"))).toBe("limited");expect(deriveClassScheduleStatus({...base,remaining:0},new Date("2026-01-01"))).toBe("full")});it("derives started completed and cancelled",()=>{expect(deriveClassScheduleStatus({...base,startDate:"2025-01-01"},new Date("2026-01-01"))).toBe("started");expect(deriveClassScheduleStatus({...base,sessionSchedule:[{id:"1",date:"2025-01-01",startTime:"10:00",endTime:"11:00",status:"completed"}]},new Date("2026-01-01"))).toBe("completed");expect(deriveClassScheduleStatus({...base,sessionSchedule:[{id:"1",date:"2099-01-01",startTime:"10:00",endTime:"11:00",status:"cancelled"}]},new Date("2026-01-01"))).toBe("cancelled")});it("supports legacy category",()=>{expect(classCategory(base)).toBe("گلیم‌بافی")});it("builds a Persian month grid",()=>{expect(jalaliMonthGrid(1405,7).filter(Boolean).length).toBe(30);expect(currentJalaliMonth(new Date("2026-09-20T00:00:00Z"))).toEqual({year:1405,month:6})});it("has responsive cards, calendar and sitemap",()=>{const page=fs.readFileSync("app/classes/schedule/page.tsx","utf8");expect(page).toContain('view==="calendar"');expect(page).toContain("rounded-2xl border");expect(page).not.toContain("<table");expect(fs.readFileSync("app/sitemap.ts","utf8")).toContain('"/classes/schedule"')})});
