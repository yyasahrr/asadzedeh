# چک‌لیست ویژگی‌ها — اسدزاده

> ساخته‌شده در پاسخ به درخواست «همه‌چیز واقعی و کاربردی شود».
> وضعیت: ✅ انجام‌شده و تست‌شده | 🟡 انجام‌شده با حالت نمایشی تا اتصال کلید واقعی | 🔲 باقی‌مانده (نیازمند اقدام شما)

## گواهی و دانلود PDF
- ✅ دانلود واقعی PDF گواهی (`/api/certificates/[code]/pdf`) — رندر فارسی با satori + resvg + pdf-lib، فونت وزیرمتن
- ✅ دکمه «دانلود PDF» در صفحه گواهی هنرجو + دکمه دانلود در جدول گواهی‌های ادمین
- ✅ صفحه اعتبارسنجی عمومی `/verify/[code]` با QR
- ✅ صدور گواهی از ادمین (فرم + ثبت در دیتابیس)

## تمرین‌ها (واقعی، نه نمایشی)
- ✅ ارسال فایل تمرین توسط هنرجو (آپلود واقعی روی سرور، حد ۱۰MB)
- ✅ مشاهده وضعیت بررسی + یادداشت استاد
- ✅ پنل بررسی تمرین‌ها در ادمین: تأیید / نیاز به اصلاح + یادداشت

## فروشگاه، سبد و پرداخت
- ✅ سبد خرید واقعی (localStorage + دکمه افزودن در همه صفحات دوره/کلاس)
- ✅ صفحه سبد با کد تخفیف (`ASAD10` = ده درصد)
- ✅ تسویه‌حساب: فرم واقعی → ثبت سفارش در دیتابیس → هدایت به درگاه
- 🟡 درگاه زرین‌پال: کد کامل اتصال نوشته شده؛ بدون کلید واقعی در «حالت نمایشی» پرداخت را شبیه‌سازی می‌کند (از ادمین → پرداخت‌ها کلید را وارد کنید)
- ✅ کال‌بک پرداخت: تأیید تراکنش، به‌روزرسانی سفارش، صفحه موفقیت/خطا
- ✅ سفارش‌های من در پنل هنرجو + مدیریت سفارش‌ها در ادمین

## احراز هویت و نقش‌ها
- ✅ ورود/ثبت‌نام واقعی با سشن کوکی امن (scrypt)، خروج
- ✅ نقش‌ها: مدیر کامل، مدیر داخلی، ویراستار محتوا، پشتیبانی سفارش‌ها، **استاد (پنل جداگانه)**، هنرجو + مدیریت کاربران در ادمین
- ✅ حساب‌های نمایشی: `09120000001/admin123` (مدیر)، `09120000002/editor123` (ویراستار)، `09120000003/support123` (پشتیبانی)، `09120000004/dyer1234` (استاد)، `09123456789/sara1234` (هنرجو؛ در دو دوره ثبت‌نام دارد)
- ✅ قفل حساب پس از تلاش‌های ناموفق، ثبت IP و مرورگر نشست‌ها، خروج از سایر نشست‌ها

## ورود دومرحله‌ای (TOTP — Google Authenticator)
- ✅ فعال‌سازی از «حساب → امنیت» با QR و کد تأیید؛ ۱۰ کد بازیابی یک‌بارمصرف (هش‌شده)
- ✅ جریان ورود: رمز → صفحه تأیید کد → نشست تأییدشده؛ محافظت در برابر استفاده مجدد از یک کد (replay)
- ✅ سیاست «اجباری برای کارکنان» از ادمین → امنیت؛ کارمند بدون ۲FA تا فعال‌سازی وارد پنل نمی‌شود
- ✅ پیاده‌سازی مطابق RFC 6238 (تست‌شده با بردارهای استاندارد)؛ secret با `APP_SECRET` روی دیسک رمزنگاری می‌شود (AES-256-GCM)

## ویدیو، جلسات دوره و حفاظت محتوا
- ✅ تعریف **تیزر** برای دوره آنلاین و کلاس حضوری (آپلود اختصاصی یا embed آپارات/یوتیوب) — نمایش در صفحه عمومی
- ✅ **آپلود جلسات** از ادمین (`/admin/courses/[slug]/lessons`): آپلود تکه‌تکه تا ۴GB، فصل‌بندی، ترتیب، مدت، «پیش‌نمایش رایگان»
- ✅ **کتابخانه ویدیو** (`/admin/videos`): فایل‌ها خارج از `public` (پوشه `data/videos/`) و فقط از طریق API امضاشده قابل دسترس‌اند
- ✅ **پلیر امن**: توکن کوتاه‌عمر HMAC (بسته به کاربر + مرورگر)، Range streaming، غیرفعال‌کردن دانلود/راست‌کلیک/PiP، تمدید خودکار توکن
- ✅ **واترمارک با شماره موبایل بیننده**: لایه متحرک روی ویدیو (همیشه) + سوزاندن دائمی روی فایل با ffmpeg (در صورت نصب)
- 🟡 **HLS و واترمارک سوخته**: کد کامل است؛ نیازمند نصب `ffmpeg` روی سرور (مسیر در ادمین → ویدیوها → تنظیمات یا `FFMPEG_PATH`)
- 🟡 **اسپات‌پلیر**: صدور خودکار لایسنس با واترمارک شماره هنرجو پس از پرداخت (`POST panel.spotplayer.ir/license/edit/`)؛ با وارد کردن API Key در ادمین فعال می‌شود، نمایش کلید/لینک در پنل هنرجو
- ✅ **پخش‌کننده هنرجو** (`/dashboard/courses/[slug]`): فهرست فصل‌ها، پیشرفت، تکمیل خودکار جلسه در ۹۰٪، ادامه از آخرین جلسه، لینک گواهی در ۱۰۰٪
- ✅ کنترل دسترسی متمرکز (`lib/access.ts`): هنرجوی ثبت‌نام‌شده، جلسه رایگان، تیزر، استاد صاحب دوره، کارکنان — و ثبت هر تلاش ناموفق در لاگ امنیتی

## پنل استاد (چند مدرس)
- ✅ افزودن مدرس نامحدود از ادمین (`/admin/instructors`) با اتصال به حساب کاربری نقش «استاد»
- ✅ پنل `/instructor`: پیشخوان، دوره‌های من (ویرایش جلسات و آپلود ویدیو)، هنرجویان و بررسی تمرین‌ها، درآمد، پروفایل عمومی
- ✅ صفحه اساتید و فرم دوره/کلاس از همین فهرست زنده می‌خوانند

## فروشگاه لوازم و پیش‌سفارش
- ✅ فروشگاه عمومی `/shop` با دسته‌بندی، صفحه محصول، گالری، مشخصات، افزودن به سبد با تعداد
- ✅ مدیریت محصول از ادمین: قیمت/قیمت قبل، **موجودی** و تنظیم سریع موجودی، backorder، وزن، روش‌های ارسال مجاز، تصاویر، فعال/ویژه
- ✅ **روش‌های ارسال** (پست، تیپاکس، تحویل حضوری…) با هزینه، آستانه ارسال رایگان و زمان تحویل — قابل ویرایش در ادمین → فروشگاه → تنظیمات
- ✅ تسویه‌حساب یکپارچه دوره + کالا: بازقیمت‌گذاری سمت سرور، رزرو موجودی/ظرفیت، آدرس فقط برای کالای فیزیکی، آزادسازی در پرداخت ناموفق
- ✅ پس از پرداخت: ثبت‌نام خودکار دوره‌ها، ساخت حساب هنرجو برای خریدار مهمان (رمز پیامکی)، پیامک تأیید، کد رهگیری پستی از ادمین
- ✅ **پیش‌سفارش ساخت** (دار قالی سفارشی و…): فرم مشخصات (ابعاد، جنس چوب…)، بیعانه درصدی، خط زمانی وضعیت (ثبت → بیعانه → ساخت → آماده → ارسال → تحویل)، پیامک هر تغییر وضعیت، پیگیری عمومی با شماره سفارش + موبایل

## لاگ و ممیزی (Audit)
- ✅ ثبت ساخت‌یافته همه رخدادها: ورود/خروج/۲FA، تغییر نقش، CRUD محتوا، آپلود و پخش ویدیو، سفارش، پیش‌سفارش، تنظیمات — با IP، مرورگر، سطح (info/warn/error/security)
- ✅ صفحه `/admin/audit`: جستجو، فیلتر سطح/عملیات/کاربر، خروجی CSV، **زنجیره هش** برای کشف دست‌کاری + دکمه اعتبارسنجی
- ✅ پیشخوان ادمین با KPI زنده (درآمد، ثبت‌نام، آماده ارسال، پیش‌سفارش باز، کم‌موجودی، رخداد امنیتی)

## اینستاگرام در صفحه اصلی
- ✅ بخش «یک نما از اینستاگرام» قبل از فوتر با embed رسمی (بدون نیاز به کلید API)؛ فهرست پست/ریلز یا پروفایل، قابل خاموش/روشن و ویرایش از ادمین → محتوا

## پنل ادمین (همه واقعی)
- ✅ دوره‌ها، کلاس‌ها، هنرجویان، سفارش‌ها، گواهی‌ها (CRUD کامل + آپلود تصویر)
- ✅ وبلاگ: ساخت/ویرایش/حذف مقاله با آپلود کاور
- ✅ کتابخانه رسانه: آپلود/حذف تصویر، انتخاب از گالری در فرم‌ها
- ✅ محتوای سایت: اطلاعیه، هیرو، آمار، معرفی درباره‌ما، اطلاعات تماس، شبکه‌ها
- ✅ نظرات: تأیید/رد نظرهای سایت
- ✅ اطلاع‌رسانی: ارسال پیامک/ایمیل همگانی (ثبت در لاگ)
- ✅ پرداخت‌ها: اتصال زرین‌پال (تست زنده دکمه) + مشاهده تراکنش‌ها
- ✅ تنظیمات: اتصال پیامک (کاوه‌نگار/قاصدک)، ایمیل (SMTP)، دامنه سایت

## پیامک، ایمیل و اطلاع‌رسانی
- 🟡 کاوه‌نگار / قاصدک: کد اتصال کامل؛ بدون کلید، پیام‌ها فقط در لاگ ادمین ذخیره می‌شوند
- 🟡 ایمیل SMTP (nodemailer): کد اتصال کامل؛ بدون تنظیمات، در لاگ ذخیره می‌شود
- ✅ فرم تماس ذخیره واقعی + خبرنامه (عضویت واقعی، جلوگیری از تکراری)

## محتوا و ظاهر همه صفحات
- ✅ هدر/فوتر/هیرو کاملاً قابل‌ویرایش از ادمین (نام برند، منو، تماس، شبکه‌ها، آمار)
- ✅ صفحه درباره‌ما، وبلاگ، جزئیات دوره آنلاین و کلاس حضوری با محتوای کامل (سرفصل، سوالات، نظرات، اشتراک‌گذاری)
- ✅ همه تصاویر از گالری محلی؛ امکان جایگزینی با آپلود از ادمین
- ✅ ریسپانسیو: جداول اسکرول موبایل، تایپوگرافی سیال گواهی، منوی موبایل

## سئو
- ✅ متادیتای کامل (title/description/OpenGraph/Twitter/canonical)، sitemap.xml، robots.txt، OG image خودکار
- ✅ JSON-LD دوره/مقاله/سوالات متداول در صفحات جزئیات

## 🔲 باقی‌مانده (اقدام شما)
0. **`APP_SECRET`** — یک مقدار تصادفی بلند در `.env` بگذارید (`openssl rand -hex 32`)؛ امضای توکن ویدیو و رمزنگاری TOTP به آن وابسته است.
0. **ffmpeg** (اختیاری) — برای HLS و واترمارک سوخته روی سرور نصب کنید و مسیر را در ادمین → ویدیوها → تنظیمات وارد کنید.
0. **اسپات‌پلیر** (اختیاری) — API Key را از پنل اسپات‌پلیر بگیرید و در ادمین → ویدیوها → تنظیمات (یا `SPOTPLAYER_API_KEY`) وارد کنید؛ شناسه دوره اسپات‌پلیر را در تنظیمات حفاظت هر دوره بنویسید.
1. **کلید واقعی زرین‌پال** — از [zarinpal.com](https://zarinpal.com) مرچنت بگیرید و در ادمین → پرداخت‌ها وارد کنید (یا `ZARINPAL_MERCHANT_ID`).
2. **کلید پیامک واقعی** — پنل کاوه‌نگار یا قاصدک بسازید و در ادمین → تنظیمات وارد کنید.
3. **SMTP واقعی** — اطلاعات هاست ایمیل را در ادمین → تنظیمات وارد کنید.
4. **دامنه نهایی** — دامنه را در ادمین → تنظیمات (`siteUrl`) ثبت کنید تا لینک‌های PDF/سئو درست شوند.
5. **دیتابیس production (اختیاری، پیشنهادی)** — فعلاً فایل `data/db.json` است؛ برای مقیاس بالا به Postgres مهاجرت کنید (رابط `lib/store.ts` متمرکز و آماده جایگزینی است).
6. **بکاپ دوره‌ای** — از پوشه‌های `data/` (شامل `data/videos/` = ویدیوهای دوره) و `public/uploads/` بکاپ بگیرید.
