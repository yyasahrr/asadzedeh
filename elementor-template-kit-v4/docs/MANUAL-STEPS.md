# گام‌های دستی پس از Import

| قالب | کار لازم |
|---|---|
| header.json | پس از Import: ویجت Nav Menu را باز کنید و منوی اصلی سایت را انتخاب کنید. لوگو در صورت نبود site logo، تصویر جایگزین قرار دهید. |
| footer.json | لینک‌های شبکه‌های اجتماعی و اطلاعات تماس را با مقادیر واقعی جایگزین کنید. |
| section-hero.json | از طریق کتابخانهٔ المنتور در هر صفحه درج شود. |
| section-cta.json | از طریق کتابخانهٔ المنتور در هر صفحه درج شود. |
| section-empty-state.json | برای وضعیت‌های بدون داده در پنل هنرجو استفاده شود. |
| section-trust.json | متن موارد را با مزایای واقعی کارگاه جایگزین کنید؛ عمداً بدون عدد است. |
| home.json | تصویر Hero و تصاویر گالری را جایگزین کنید. |
| about.json | روایت کارگاه و بخش «نسبت فن با سنت» با اطلاعات واقعی تکمیل شود. |
| contact.json | Action After Submit فرم و کلید API نقشه را تنظیم کنید. |
| works.json | پس از Import، Loop Item اثر را در Dynamic Grid انتخاب کنید. |
| single-artwork.json | فیلدهای ACF را در ویجت‌های مربوط انتخاب کنید. |
| blog.json | اگر از Theme Builder استفاده می‌کنید، الگوی آرشیو را به نوشته‌ها متصل کنید. |
| instructors.json | در صورت استفاده از CPT مدرس، Loop Item مربوط را انتخاب کنید. |
| single-instructor.json | فیلدهای ACF را متصل کنید. |
| gallery.json | تصاویر واقعی کارگاه را جایگزین کنید. |
| faq.json | پرسش‌ها را با پرسش‌های واقعی کارگاه تکمیل کنید. |
| support.json | روش واقعی پاسخ‌گویی را جایگزین کنید. |
| verify.json | نیازمند backend/asadzadeh-kit-shortcodes.php |
| privacy.json | پیش‌نویس حقوقی — پیش از انتشار بازبینی شود. |
| terms.json | پیش‌نویس حقوقی — پیش از انتشار بازبینی شود. |
| rules.json | پیش‌نویس حقوقی — پیش از انتشار بازبینی شود. |
| refund.json | پیش‌نویس حقوقی — پیش از انتشار بازبینی شود. |
| shipping.json | پیش‌نویس حقوقی — پیش از انتشار بازبینی شود. |
| single-course.json | قیمت، سطح و مدرس از طریق az_course_meta خوانده می‌شود؛ در صورت نبود فیلد، مقادیر REPLACE جایگزین کنید. |
| enrolled-course.json | این صفحه باید به مسیر یادگیری LearnDash متصل شود. |
| learning-paths.json | در صورت نبود CPT، فهرست ثابت نمایش داده می‌شود. |
| learning-path-single.json | Slugهای فارسی باید با تنظیمات پیوند یکتا (UTF-8) بررسی شوند. |
| workshops.json | دستهٔ محصول با slug دقیق workshop ایجاد شود. |
| class-single.json | ظرفیت = موجودی محصول. |
| single-product.json | قالب کلاس حضوری اولویت بالاتری برای دستهٔ workshop دارد. |
| dashboard.json | دسترسی این صفحه باید به کاربران واردشده محدود شود. |
| assignments.json | ارسال فایل باید با تنظیمات تکلیف LearnDash هماهنگ شود. |
| account-security.json | فرم‌های تغییر شماره/رمز با shortcode واقعی Digits جایگزین شود. |
| cart-empty.json | در صورت استفاده از ووکامرس، این صفحه به‌عنوان جایگزین خالی نمایش داده می‌شود. |
| checkout.json | سربرگ ساده‌شده برای کاهش حواس‌پرتی؛ هدر اصلی را غیرفعال کنید. |
| checkout-error.json | نمونهٔ خطاها برای بررسی ظاهری است. |
| payment-success.json | این صفحه را به Thank You page ووکامرس متصل کنید. |
| my-account.json | پیوندها با پنل هنرجو یکسان شده است. |
| auth.json | شورتکد Digits را با نسخهٔ نصب‌شده تطبیق دهید. |
| otp.json | اگر Digits مرحلهٔ OTP را جداگانه ارائه نمی‌دهد، این صفحه را به جریان اصلی متصل نکنید. |
| search.json | در صورت استفاده از قالب Search Results المنتور پرو، این صفحه را به آن متصل کنید. |
| not-found.json | در صورت فعال‌بودن قالب ۴۰۴ المنتور پرو، از همان استفاده کنید. |
| maintenance.json | در زمان فعال‌سازی، این صفحه را به‌عنوان صفحهٔ نگه‌دارنده تنظیم کنید. |
| loop-course.json | پس از Import، این Loop Item را در ویجت گرید انتخاب کنید. |
| loop-product.json | قیمت در صورت نیاز با ویجت Price به کارت اضافه شود. |
| loop-artwork.json | فیلد ACF تکنیک را در صورت نیاز اضافه کنید. |

## گام‌های عمومی

1. ثبت دو فونت `Neirizi` و `Peyda` در Elementor › Custom Fonts (بدون بسته‌بندی فونت).
2. انتخاب منوی اصلی در ویجت Nav Menu قالب سربرگ.
3. تنظیم Action After Submit فرم تماس (ایمیل/وب‌هوک) و جایگزینی ایمیل پیش‌فرض.
4. جایگزینی همهٔ مقادیر `REPLACE:` با دادهٔ واقعی (تلفن، نشانی، قیمت، تاریخ کلاس‌ها، نام مدرسان).
5. انتخاب Loop Item مربوط در Dynamic Gridهای آثار و مدرسان.
6. تنظیم شرایط نمایش طبق `THEME-BUILDER-CONDITIONS.md`.
7. اتصال صفحات پرداخت موفق/ناموفق به Thank You page ووکامرس.
8. محدود کردن دسترسی صفحات پنل هنرجو به کاربران واردشده.
9. بررسی RTL و ریسپانسیو طبق `RESPONSIVE-QA.md`.
10. بازبینی حقوقی متن‌های `privacy`، `terms`، `rules`، `refund` و `shipping`
    پیش از انتشار (این متن‌ها پیش‌نویس هستند).
